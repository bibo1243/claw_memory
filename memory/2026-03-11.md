# 2026-03-11

- **LINE 對接 (龍蝦)**: Gary 將 OpenClaw (龍蝦) 與 LINE 官方帳號進行對接。
  - 已將 `channelSecret` 和 `channelAccessToken` 透過 `config.patch` 寫入 OpenClaw 設定檔中的 `channels.line`。
  - 目前因 OpenClaw 運行於 MacBook Air 地端，正準備討論使用 ngrok, Cloudflare Tunnel 或 Tailscale Funnel 來暴露 Webhook 網址供 LINE 伺服器連接。
- **錯誤排除**: 
  - 遇到 Gemini API Key 過期的報錯 (稍後似乎自行恢復)。
  - 遇到傳送圖片時觸發了舊模型 (`google-antigravity/claude-sonnet-4-20250514`) 不存在的錯誤，研判是背景任務或 Fallback 設定中殘留了舊配置。暫時跳過不理會。
